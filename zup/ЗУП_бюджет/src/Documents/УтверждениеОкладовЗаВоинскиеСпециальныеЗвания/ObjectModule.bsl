
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	// Проведение документа
	ПроведениеСервер.ПодготовитьНаборыЗаписейКРегистрацииДвижений(ЭтотОбъект);
	
	ДанныеДляПроведения = ДанныеДляПроведения();
	ВоинскиеСпециальныеЗвания.СформироватьДвиженияОкладовПоВоинскимСпециальнымЗваниям(Движения, ДанныеДляПроведения.ОкладыПоВоинскимСпециальнымЗваниям);
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	ЗарплатаКадры.ПроверитьКорректностьДаты(Ссылка, ДатаВступленияВСилу, "Объект.ДатаВступленияВСилу", Отказ, НСтр("ru='Дата вступления в силу'"), , , Ложь);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ДанныеДляПроведения()
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	Запрос.Текст = "ВЫБРАТЬ
	               |	УтверждениеОкладовЗаВоинскиеСпециальныеЗванияОклады.Ссылка.ДатаВступленияВСилу КАК Период,
	               |	УтверждениеОкладовЗаВоинскиеСпециальныеЗванияОклады.ВоинскоеСпециальноеЗвание,
	               |	УтверждениеОкладовЗаВоинскиеСпециальныеЗванияОклады.Размер
	               |ИЗ
	               |	Документ.УтверждениеОкладовЗаВоинскиеСпециальныеЗвания.Оклады КАК УтверждениеОкладовЗаВоинскиеСпециальныеЗванияОклады
	               |ГДЕ
	               |	УтверждениеОкладовЗаВоинскиеСпециальныеЗванияОклады.Ссылка = &Ссылка";
				   
	РезультатЗапроса = Запрос.Выполнить();
	
	ДанныеДляПроведения = Новый Структура;
	
	ОкладыПоВоинскимСпециальнымЗваниям = РезультатЗапроса.Выгрузить();
	ДанныеДляПроведения.Вставить("ОкладыПоВоинскимСпециальнымЗваниям", ОкладыПоВоинскимСпециальнымЗваниям);
	
	Возврат ДанныеДляПроведения;
	
КонецФункции

#КонецОбласти

#КонецЕсли
